import { AccountSettingsPageContent } from "@/components/settings/account-settings-page";

export const metadata = {
  title: "Account Settings - NIRNAY",
};

export default function AccountSettingsRoutePage() {
  return <AccountSettingsPageContent />;
}
