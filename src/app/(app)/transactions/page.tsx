import { TransactionsPageContent } from "@/components/transactions/transactions-page";

export const metadata = {
  title: "Transactions - NIRNAY",
};

export default function TransactionsPage() {
  return <TransactionsPageContent />;
}
